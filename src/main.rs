#![allow(dead_code)]
#![allow(unused_imports)]
#![feature(box_patterns)]

extern crate prusti_contracts;
use prusti_contracts::*;

mod external_spec;
mod range_inclusive;
// mod passes;
// mod failing;
mod failing2;

fn main() {
}
